namespace CompanyService.Core.Enums
{
    public enum AccountType
    {
        Asset = 0,      // Activo
        Liability = 1,  // Pasivo
        Equity = 2,     // Patrimonio
        Revenue = 3,    // Ingresos
        Expense = 4     // Gastos
    }
}
